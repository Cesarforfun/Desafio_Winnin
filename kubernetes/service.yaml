apiVersion: v1
kind: Service
metadata:
  name: node-app-service
  namespace: production
  labels:
    app: node-app
spec:
  selector:
    app: node-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5005
  type: ClusterIP